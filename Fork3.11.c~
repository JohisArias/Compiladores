//Programa 3.11. Ejemplo de utilización de exit y atexit.
#include <stdio.h>
#include <stdlib.h>
void main(void)
{
	printf("Fin de la ejecución del proceso %d\n", getpid());
	return;
}

int main(void)
{
	if (atexit(fin))
		perror("atexit");
	exit(1);
	exit(0);
}
